All CRUD pages should:

- use the existing Card layout
- use MUI components
- use the existing page header
- use the existing mobile layout
- use the existing delete/update interaction
- use fetch() through the existing API pattern
- display loading/error states