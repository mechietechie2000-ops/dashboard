Rules:
  - Never modify DB schema without explicitly stating it
  - API responses use camelCase
  - Database columns use snake_case
  - All CRUD routes follow existing route pattern
  - React pages use existing Card/Table components
  - Run npm build after frontend changes
  - Run backend tests after API changes